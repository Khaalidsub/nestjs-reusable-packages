# Application Configuration
PORT=3001

# Redis Streams Configuration
# Single consumer group and consumer for all streams
CONSUMER=consumer-2
GROUP=nestjs_group

# Primary stream (for backward compatibility)
STREAM=mystream

# Multi-stream example (comma-separated list)
# STREAMS=user-events,order-events,notification-events